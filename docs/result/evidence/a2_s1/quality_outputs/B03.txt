I'll show you how to refactor a function with nested if statements using early returns, which improves readability and reduces complexity.

## Before: Nested Ifs

```javascript
function processUser(user, permissions, isActive) {
    if (user) {
        if (permissions) {
            if (isActive) {
                if (user.age >= 18) {
                    if (permissions.canEdit) {
                        if (permissions.canDelete) {
                            return {
                                status: 'authorized',
                                message: 'User can edit and delete',
                                user: user
                            };
                        } else {
                            return {
                                status: 'partial',
